using Autofac;

namespace Demo.MusicLibrary.Api.BusinessLogic.Autofac
{
    public class MainModule : Module
    {
        // ReSharper disable once RedundantOverriddenMember
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
